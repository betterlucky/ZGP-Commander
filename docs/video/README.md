# Build Week video assets

The published 1920×1080 master is `zgp-commander-build-week-2026.mp4` with a
runtime of 2:52. It is kept outside the source repository and contains the
deterministic demo route, a repository/architecture card grounded in a passing
`npm run check`, and the required ElevenLabs credit on the closing card.

The ten scene-aligned narration clips are ignored binary assets; their
generation details are in [`voiceover`](voiceover/README.md). Raw browser
recordings are also kept locally in the ignored `capture/` directory so the
public repository does not absorb large intermediate media files.

Publishing copy and judge links are maintained in [`../SUBMISSION.md`](../SUBMISSION.md).
